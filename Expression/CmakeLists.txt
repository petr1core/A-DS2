set(SUBPROJECT_NAME Expression)

include_directories(${CMAKE_CURRENT_SOURCE_DIR}/${SUBPROJECT_NAME}Lib)
add_subdirectory(${SUBPROJECT_NAME})
add_subdirectory("${SUBPROJECT_NAME}Test")

## file(GLOB srcs "*.cpp")
## add_executable(${PROJECT_NAME} ${srcs})
## target_link_libraries(${PROJECT_NAME} ${ExpressionLib} gtest)

## project(${PROJECT_NAME})

## set(CMAKE_CONFIGURATION_TYPES "Debug;Release" CACHE STRING "Configs" FORCE)
## if(NOT CMAKE_BUILD_TYPE)
##   set(CMAKE_BUILD_TYPE Release)
## endif()